import type { BlogPost } from './types';

export const blog2: BlogPost = {
  id: 2,
  title: "My First Steps into Backend Development: Learning CodeIgniter 4",
  date: "2026/04/29",
  readTime: 10,
  content: `After applying for a position with the City Government of Biñan, I was accepted and given the opportunity to learn and apply my skills in a real working environment. My first task was to familiarize myself with the project's development environment by configuring XAMPP and setting up a MySQL database. From there, my responsibilities grew steadily: I learned the routing mechanics of CodeIgniter 4, developed a functional backend login API, collaborated with the design team to help formulate the system's core logic and structural architecture, mapped out project workflows visually using Data Flow Diagrams, and began initial development on the backend interface responsible for handling Excel file imports.
The stack we used was CodeIgniter 4 running on PHP 8.2, a framework built on the classic MVC architecture. At first it felt like a maze of folders and conventions, but everything clicked once I understood the request lifecycle: the path a request travels from the browser, through the framework, and back out as a response. Every interaction with the system follows that same journey, so learning it gave me a mental map of the entire application.
The journey starts at a single entry point. Every request, no matter where it is headed, passes through one front door where the framework wakes up, loads its configuration, and prepares the application. The router then reads the incoming address and decides which part of the codebase should handle it, much like a receptionist directing a visitor to the right office.
Before the request reaches its destination, it passes through a layer of filters that act as security checkpoints. This is where the system verifies who you are and what you are allowed to do, so individual features never need to repeat those checks themselves. Once cleared, the request arrives at a controller, which I came to think of as a traffic cop: it looks at what the user sent, decides what needs to happen, and delegates the actual work. Data responsibilities belong to the model, which is the only layer that talks to the database, while the view takes whatever the controller hands over and turns it into the page the user finally sees. Each layer has one job, and each one hands off cleanly to the next.
What impressed me most was how the framework keeps all of these layers synchronized. Routes must agree with the controllers they point to, controllers only see data through models, and views can only display what controllers explicitly give them. Supporting pieces such as centralized configuration, shared services, reusable helpers, and versioned database migrations all follow the same philosophy: define things once, in one place, and let every layer read from the same source of truth. Once I understood that discipline, debugging stopped feeling like guesswork and started feeling like following a trail.
This was really the start of my journey: learning a new stack alongside React.js, understanding the different components involved, and seeing how each piece connects and functions within a real, working system.`,
};
